<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/holo_red_dark"/> <!-- Red background -->
    <corners android:radius="12dp"/> <!-- Optional: rounded corners -->
    <padding
        android:left="12dp"
        android:top="6dp"
        android:right="12dp"
        android:bottom="6dp"/> <!-- Optional: padding inside the badge -->
</shape>
